spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/seating}
    username: ${DB_USER:seating}
    password: ${DB_PASSWORD:seating}
  jackson:
    default-property-inclusion: non_null

server:
  port: ${SERVER_PORT:8080}
